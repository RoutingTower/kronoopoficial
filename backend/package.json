{
  "name": "kronoop-backend",
  "version": "0.1.0",
  "description": "API do KronoOP — servida para o frontend, com Firebase (Firestore + Auth) como banco de dados.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "render:sync-env": "node scripts/sync-render-env.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.5.0"
  }
}
